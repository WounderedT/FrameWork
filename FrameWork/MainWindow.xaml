<Window x:Class="FrameWork.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:local="clr-namespace:FrameWork"
        mc:Ignorable="d"
        Title="FrameWork" Height="750" Width="850" ResizeMode="CanMinimize">

    <i:Interaction.Behaviors>
            <local:EventToCommandBehavior Command="{Binding CloseMainWindowCommand}" Event="Closing" PassArguments="True"/>
    </i:Interaction.Behaviors>
    <Window.Resources>
        <ResourceDictionary Source="MainWindowDictionary.xaml"/>
    </Window.Resources>
    <Grid Background="Red" Name="MainWindowGrid">
        <TabControl x:Name="tabControl" HorizontalAlignment="Center" Height="700" VerticalAlignment="Center" Width="824"
                    ItemsSource="{Binding Tabs}" SelectedItem="{Binding SelectedTab}" >
            <!--<TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Header}" />
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    
                </DataTemplate>
            </TabControl.ContentTemplate>-->
        </TabControl>
    </Grid>
</Window>
